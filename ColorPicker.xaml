<UserControl x:Name="cpcColor" x:Class="WPF_LED_Controller.ColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="201
             " d:DesignWidth="201">
    <Grid Margin="0,0,2,2" Width="201" Height="201">
        <Image x:Name="image" 
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top" Margin="2"
                        />
        <Canvas x:Name="CanColor"  Width="201" Height="201" 
                       HorizontalAlignment="Left"
                       VerticalAlignment="Top" Margin="2,2,0,0" MouseLeftButtonDown="CanColor_MouseLeftButtonDown" MouseLeftButtonUp="CanColor_MouseLeftButtonUp" MouseMove="CanColor_MouseMove"  >
            <Canvas.Background>
                <ImageBrush/>
            </Canvas.Background>
            <Ellipse x:Name="EpPointer" Width="8" 
                       Height="8" Stroke="Black" Fill="White" MouseLeftButtonDown="EpPointer_MouseLeftButtonDown" MouseLeftButtonUp="EpPointer_MouseLeftButtonUp" MouseMove="EpPointer_MouseMove" 
                       />
        </Canvas>

    </Grid>
</UserControl>
